# Reprobuild Nix Packages

This repository contributes Nix realizations for package interfaces owned by
other repositories. Every contribution must pin the target public interface
fingerprint. Do not copy or redefine package interfaces here.

Changes land on `dev`. Keep public commit messages user-facing.

